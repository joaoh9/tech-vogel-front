module.exports = {
  root: true,
  env: {
    node: true,
  },
  extends: [ 'plugin:vue/essential', 'eslint:recommended' ],
  parserOptions: {
    parser: 'babel-eslint',
  },
  rules: {
    'no-console': process.env.VUE_APP_NODE_ENV === 'production' ? 'warn' : 'off',
    'no-debugger': process.env.VUE_APP_NODE_ENV === 'production' ? 'warn' : 'off',
    'max-len': [
      'warn',
      {
        code: 120,
        tabWidth: 2,
        ignoreStrings: true,
        ignoreComments: true,
        ignoreUrls: true,
        ignoreRegExpLiterals: true,
        ignoreTemplateLiterals: true,
      },
    ],
    eqeqeq: process.env.VUE_APP_NODE_ENV === 'production' ? 'warn' : 'off',
    'max-classes-per-file': process.env.VUE_APP_NODE_ENV === 'production' ? 'warn' : 'off',
    'no-multi-spaces': process.env.VUE_APP_NODE_ENV === 'production' ? 'warn' : 'off',
    'array-bracket-spacing': [
      process.env.VUE_APP_NODE_ENV === 'production' ? 'warn' : 'warn',
      'always',
      { singleValue: false },
    ],
    'comma-dangle': [ 'warn', 'always-multiline' ],
    'comma-style': [ 'warn', 'last' ],
    'eol-last': [ 'warn', 'always' ],
    'func-call-spacing': [ 'warn', 'never' ],
    'key-spacing': [ 'warn', { beforeColon: false, afterColon: true } ],
    'no-trailing-spaces': 'warn',
    'no-whitespace-before-property': 'warn',
    quotes: [ process.env.NODE_ENV === 'production' ? 'warn' : 'warn', 'single' ],
    'space-infix-ops': 'warn',
    'no-var': 'warn',
    'prefer-const': 'warn',
    indent: [
      'warn',
      2,
      {
        SwitchCase: 1,
        VariableDeclarator: 1,
        MemberExpression: 1,
        ArrayExpression: 1,
        ObjectExpression: 1,
        ImportDeclaration: 1,
        flatTernaryExpressions: true,
        ignoreComments: false,
      },
    ],
    'no-unused-vars': process.env.VUE_APP_NODE_ENV === 'production' ? 'warn' : 'warn',
  },
};
